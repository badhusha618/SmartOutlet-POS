# ======================================
# SmartOutlet Product Service Configuration
# ======================================

# Server Configuration
server.port=8083
server.address=0.0.0.0
server.servlet.context-path=/products
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true

# Application Information
spring.application.name=product-service
management.endpoints.web.exposure.include=health,info,metrics

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/smartoutlet_product
spring.datasource.username=postgres
spring.datasource.password=smartoutlet123
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.open-in-view=false

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=true

# Kafka Configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.retries=3
spring.kafka.producer.acks=all
spring.kafka.producer.batch-size=16384
spring.kafka.producer.linger-ms=5
spring.kafka.producer.buffer-memory=33554432

spring.kafka.consumer.group-id=product-service-group
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false
spring.kafka.listener.ack-mode=manual

# Redis Configuration
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=smartoutlet123
spring.redis.timeout=2000
spring.redis.jedis.pool.max-active=10
spring.redis.jedis.pool.max-idle=5
spring.redis.jedis.pool.min-idle=2

# External Service URLs
auth.service.url=http://localhost:8081

# Product Configuration
app.product.low-stock-threshold=${APP_PRODUCT_LOW_STOCK_THRESHOLD:10}
app.product.enable-stock-alerts=${APP_PRODUCT_ENABLE_STOCK_ALERTS:true}
app.product.auto-reorder=false
app.product.image-upload-path=/uploads/products
app.product.max-image-size=5MB

# Logging Configuration
logging.level.com.smartoutlet.product=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.cache=DEBUG
logging.level.org.flywaydb=INFO

# OpenAPI/Swagger Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.doc-expansion=none
springdoc.swagger-ui.disable-swagger-default-url=true

# Security Configuration
app.security.cors.allowed-origins=http://localhost:3000,http://localhost:8080
app.security.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.security.cors.allowed-headers=*
app.security.cors.allow-credentials=true

# Actuator Configuration
management.endpoint.health.show-details=always
management.health.db.enabled=true
management.health.redis.enabled=true
management.health.kafka.enabled=true

# Profile-specific configurations
# spring.profiles.active is set via command line or environment variable

# ======================================
# Docker Profile Configuration
# ======================================
---
spring.config.activate.on-profile=docker

# Database Configuration for Docker
spring.datasource.url=jdbc:postgresql://localhost:5432/smartoutlet_product

# Kafka Configuration for Docker
spring.kafka.bootstrap-servers=localhost:9092

# Redis Configuration for Docker
spring.redis.host=localhost

# External Service URLs for Docker
auth.service.url=http://localhost:8081

# CORS Configuration for Docker
app.security.cors.allowed-origins=http://localhost:3000,http://localhost:8080,http://api-gateway:8080

# Logging for Docker
logging.level.root=INFO
logging.level.com.smartoutlet.product=INFO
logging.level.org.springframework.kafka=WARN

server.address=0.0.0.0

# Add these lines to the existing application.properties file

# Cache Configuration
spring.cache.type=redis
spring.cache.redis.time-to-live=1800000
spring.cache.redis.cache-null-values=false
logging.level.org.springframework.cache=DEBUG